（1） http://202.116.76.22/netdisk1/default.aspx?vm=13mo
      每个小组采用自己（最小学号）的目录，可以建子目录。该目录只能上传和覆盖文件，不能下载和删除文件。
 
（2）下面的上传文件包和MySql连接包已经放在Tomcat/lib目录下，不用另外上传：
     commons-fileupload-1.2.1.jar，commons-io-1.4.jar，mysql-connector-java-5.1.36-bin.jar

（3）数据库采用202.116.76.22，端口号为3306，用户名为user，密码为123456
     为了防止各组之间误操作，该用户被限制了操作权限，只能建立数据库和表，不能删除数据库和表。